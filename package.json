{
  "name": "neos-media-ui",
  "version": "1.0.0",
  "license": "GNU GPLv3",
  "private": true,
  "engines": {
    "node": "14"
  },
  "scripts": {
    "postinstall": "patch-package",
    "lint": "tsc --noEmit && eslint 'Resources/Private/JavaScript/**/*.{js,jsx,ts,tsx}'",
    "test": "yarn workspace backend-module run test:e2e",
    "dev": "yarn workspace backend-module run dev",
    "watch": "concurrently -k \"yarn watch:module\" \"yarn watch:ui-plugin\"",
    "build": "yarn workspaces run build"
  },
  "browserslist": [
    "> 1%",
    "Last 2 versions"
  ],
  "workspaces": [
    "Resources/Private/JavaScript/*"
  ]
}
